DATABASE_URL="file:./dev.db"
AUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Продакшен: при деплое через GitHub Actions .env на сервере собирается из секретов
# (AUTH_SECRET, NEXTAUTH_URL, DATABASE_URL) — руками класть .env на VPS не обязательно.
